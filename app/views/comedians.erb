<% @comedians.each do |comedian| %>
  <h1><%= comedian.name%></h1>
  <p>Age: <%= comedian.age%></p>
  <% @specials_list = @specials.where(comedian_id: comedian.id) %>
  <ul> Specials:
    <% @specials_list.each do |special| %>
    <li><%= special.name %></li>
    <% end %>
  </ul>
  <% end %>
